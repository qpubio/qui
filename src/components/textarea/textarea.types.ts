/** Semantic textarea color accents (excluding invalid/error override via `isInvalid`). */
export type TextareaPaletteColor =
  | "default"
  | "primary"
  | "secondary"
  | "info"
  | "debug"
  | "warning"
  | "success"
  | "error"
  | "fatal";

export type TextareaVisualVariant = "bordered";
