/** Semantic input color accents (excluding invalid/error override via `isInvalid`). */
export type InputPaletteColor =
  | "default"
  | "primary"
  | "secondary"
  | "info"
  | "debug"
  | "warning"
  | "success"
  | "error"
  | "fatal";

export type InputVisualVariant = "bordered";
